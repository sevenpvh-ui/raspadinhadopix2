<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Raspadinha Premium</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <div class="app-wrapper">
        <header>
            <div class="logo-area">
                <i class="fa-brands fa-pix"></i>
                <span>Raspadinha<strong class="highlight">PIX</strong></span>
            </div>
            <p class="subtitle">Sistema de Premiação Instantânea</p>
        </header>

        <main class="game-card">
            <div class="status-bar">
                <span class="status-text">Área de Raspagem Segura</span>
                <div class="secure-icon"><i class="fa-solid fa-lock"></i></div>
            </div>

            <div class="scratch-area-wrapper">
                <div class="scratch-card" id="scratchCard">
                    <div class="scratch-overlay" id="scratchOverlay"></div>
                    <div class="prize-display" id="prizeDisplay">
                        <div class="prize-text">R$ ???</div>
                    </div>
                </div>
            </div>

            <div class="controls">
                <button class="btn-primary" id="scratchBtn">
                    <i class="fa-solid fa-coins"></i> Raspar Agora
                </button>
                <button class="btn-secondary" id="resetBtn">
                    <i class="fa-solid fa-rotate-right"></i> Tentar Novamente
                </button>
            </div>
        </main>

        <div class="prizes-panel">
            <h3>Tabela de Premiação</h3>
            <div class="prizes-grid">
                <% premios.forEach(premio => { %>
                    <div class="prize-pill <%= premio.valor.includes('R$') ? 'win' : 'neutral' %>">
                        <span class="dot"></span>
                        <%= premio.valor %>
                    </div>
                <% }); %>
            </div>
        </div>

        <div class="result-overlay" id="resultPopup">
            <div class="result-modal">
                <div class="modal-icon" id="modalIcon">🎉</div>
                <h2 id="popupTitle">Resultado</h2>
                <p class="modal-subtitle">O seu prémio foi:</p>
                <div class="prize-amount-box">
                    <span id="prizeAmount"></span>
                </div>
                <button class="btn-modal" id="closePopup">Confirmar</button>
            </div>
        </div>

        <footer>
            <p>&copy; 2024 SevenPVH Systems. Todos os direitos reservados.</p>
        </footer>
    </div>

    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/game.js"></script>
</body>
</html>
